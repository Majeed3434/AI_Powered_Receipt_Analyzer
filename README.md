# ⚡ AI-Powered Receipt Analyzer
A high-speed receipt processing tool built with **FastAPI**, **Streamlit**, and **Groq Cloud (Llama 3.2 Vision)**.

## How to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Start Backend: `uvicorn main:app --reload`
3. Start Frontend: `streamlit run app.py`